# Algumas informações

O website está escrito em express.js, com JavaScript puro,
HTML, CSS e Tailwinds.

Ele possui express-generator para trazer uma estrutura básica.

O comando de inicialização é:
`$ DEBUG=myapp:* npm start`